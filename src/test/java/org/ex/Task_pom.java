package org.ex;



import java.io.IOException;

import org.openqa.selenium.WebDriver;

public class Task_pom extends BaseClass {

	 public static void main(String[] args) throws InterruptedException, IOException {
/*		 WebDriver driver=Launch();
		 openpage("http://www.adactin.com/HotelApp/");
		big();
		LoginPojo l=new LoginPojo();
		
		input(l.getUsername(), read(1, 0));
		input(l.getPass(), read(1, 1));
		btn(l.getLgn());
		
		input(l.getLoc(), read(1,2));
		input(l.getHotel(),read(1,3));
		input(l.getType(), read(1,4));
		input(l.getNumroom(), read(1,5));
		input(l.getCheckin(), read(1,6));
		input(l.getCheckout(), read(1, 7));
		input(l.getAdult(), read(1,8));
		input(l.getChild(), read(1,9));
		btn(l.getSearch());
		btn(l.getRadio());
		btn(l.getSearch2());
		input(l.getName1(), read(1,10));
		input(l.getName2(), read(1,11));
		input(l.getAddress(),read(1,12));
		input(l.getCrenum(), read(1,13));
		input(l.getCretype(), read(1,14));
		input(l.getExmon(), read(1,15));
		input(l.getExmyr(), read(1,16));
		input(l.getCcv(), read(1,17));
		btn(l.getBook());
     
		Thread.sleep(7000);
		
		attr(l.getOrder());
		
		
		
	 }

		
		*/
		
		
		
		
		
		
		
		
		
		
		
		
		

	WebDriver driver=Launch();
	openpage("https://www.facebook.com");
	big();
    
    LoginPojo l=new LoginPojo();
    
  //  input(l.getTxtemail(),"divya");
   /* input(l.getTxtpass(), "bharathi");
    //btn(l.getBtnlgn());
    
    navigate();
    back();
    Thread.sleep(3000);
    forward();
    refresh();*/*/
    
}
}